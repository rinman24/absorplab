[project]
name = "absorplab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rich Inman, PhD", email = "rinman24@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.2",
    "scipy>=1.18.1",
]

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
